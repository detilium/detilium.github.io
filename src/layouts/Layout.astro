---
type Props = {
	title?: string;
	description?: string;
}

const {
	title = "Portfolio - Coming Soon",
	description = "Portfilo coming soon."
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <title>{title}</title>

    <!-- Social preview (optional) -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content="I’m building something new. Check back soon." />
    <meta property="og:type" content="website" />
  </head>

  <body>
    <div class="bg" aria-hidden="true"></div>

    <main class="wrap">
      <slot />
    </main>
  </body>
</html>